set @r1=0, @r2=0, @r3=0, @r4=0;
select min(Doctor), min(Professor), min(Singer), min(Actor)
from(
  select case when Occupation='Doctor' then (@r1:=@r1+1)
            when Occupation='Professor' then (@r2:=@r2+1)
            when Occupation='Singer' then (@r3:=@r3+1)
            when Occupation='Actor' then (@r4:=@r4+1) end as RowNumber,
    case when Occupation='Doctor' then Name end as Doctor,
    case when Occupation='Professor' then Name end as Professor,
    case when Occupation='Singer' then Name end as Singer,
    case when Occupation='Actor' then Name end as Actor
  from OCCUPATIONS
  order by Name
) Temp
group by RowNumber

/*This question requires a SQL pivot table. 

The logic of this query is:
Reshape Occupation into 4 columns with their index in each occupation, this leads to Temp table (which has many NULL).
Then group Temp by index and select corresponding Name for each Occupation.
min()/max() will return a name for specific index and specific occupation. If there is a name, it will return it, if not, return NULL.

You can just run the Temp table to see what it looks like, don’t forget to include set statement. 
It will be very helpful if you break down the code and see the result.
